# Local dev connects to the Neon `dev` branch (NOT staging/production).
# Use the DIRECT (non -pooler) connection string; keep channel_binding=require.
# staging -> Railway staging env, production -> Railway production env. See .github/DEPLOYMENT.md
DATABASE_URL=
SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=

MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=
MAIL_PORT=
MAIL_SERVER=
MAIL_STARTTLS=
MAIL_SSL_TLS=